/**
 * PHASE 0 — intentionally empty.
 *
 * No business tables (users, courses, lives, quizzes...) are created in this
 * phase. The file exists so Drizzle Kit tooling (`db:generate`, `db:push`,
 * `db:migrate`) is configured and ready for PHASE 1.
 */
export {};
